# AUDIT_PROJECT_CONFIG.yaml
# Generated: 2025-12-12
# Sprint: Phase 1.5 - Scripts & Project Config Audit
# Task: Audit project configuration files

audit:
  scope: project_configuration_files
  generated_at: "2025-12-12T10:30:00+00:00"
  criteria_version: "1.0"

  summary:
    files_audited: 6
    total_size_bytes: 8645
    average_quality_score: 82.3
    grade_distribution:
      A: 2
      B: 3
      C: 1
      D: 0
      F: 0
    critical_issues:
      - ".coveragerc source path is 'framework' but package is 'vibey'"
      - "pytest.ini coverage threshold (90%) conflicts with actual coverage (28%)"
    recommendations:
      - "Update .coveragerc source to 'vibey' to match package restructure"
      - "Lower coverage threshold to realistic value (50-60%) and improve incrementally"
      - "Align Black line-length between pyproject.toml (100) and pre-commit (120)"

  files:
    # ============================================
    # pyproject.toml - Python Project Configuration
    # ============================================
    - path: pyproject.toml
      size_bytes: 2406
      config_type: python_project
      purpose: "Python project metadata, dependencies, and tool configurations (PEP 621)"

      completeness:
        required_fields:
          total: 8
          present: 8
          missing: []
          coverage_percent: 100
        optional_fields:
          available:
            - maintainers
            - urls (project.urls)
            - entry_points
          used:
            - urls
            - optional-dependencies
            - scripts
          unused:
            - maintainers
        settings_documented: false  # No inline comments
        completeness_score: 33  # 33/35

      correctness:
        syntax_valid: true
        syntax_errors: []
        values_valid: true
        invalid_values: []
        consistency:
          internally_consistent: true
          issues: []
        correctness_score: 40  # 40/40

      best_practices:
        follows_standard: true
        standard_name: "PEP 621"
        compliance_level: full
        deviations: []
        security:
          sensitive_data_exposed: false
          should_be_gitignored: false
          concerns: []
        maintainability:
          easy_to_understand: true
          well_organized: true
          issues: []
        modern_practices:
          uses_modern_format: true
          deprecated_patterns: []
          recommended_updates:
            - "Consider adding py.typed to package-data if not already"
        best_practices_score: 24  # 24/25

      sections_audit:
        build_system:
          present: true
          backend: "setuptools.build_meta"
          requires: ["setuptools>=61.0", "wheel"]
          valid: true

        project:
          name: "vibey-framework"
          version: "2.5.0"
          description: present
          readme: "README.md"
          requires_python: ">=3.9"
          license: "MIT"
          authors: present
          keywords: present (7 keywords)
          classifiers: present (10 classifiers)

        dependencies:
          runtime:
            - pyyaml>=6.0
            - jinja2>=3.1.0
            - click>=8.1.0
            - rich>=13.0.0
            - pydantic>=2.0.0
            - jsonschema>=4.0.0
            - python-ulid>=3.0.0
          version_pinning: ranges
          count: 7

        optional_dependencies:
          db: [sqlalchemy>=2.0.0]
          dev: [pytest>=7.0.0, pytest-cov>=4.0.0, black>=23.0.0, ruff>=0.1.0, mypy>=1.0.0]
          all: [sqlalchemy>=2.0.0]

        tool_sections:
          - tool: setuptools
            configured: true
            settings: [packages.find, package-data]
          - tool: black
            configured: true
            settings: [line-length=100, target-version=py39]
          - tool: ruff
            configured: true
            settings: [line-length=100, target-version=py39, select]
          - tool: mypy
            configured: true
            settings: [python_version=3.9, warn_return_any, warn_unused_configs, disallow_untyped_defs]
          - tool: pytest
            configured: true
            settings: [testpaths, python_files, addopts]

      config_quality:
        completeness: 33
        correctness: 40
        best_practices: 24
        total: 97
        grade: A

    # ============================================
    # pytest.ini - Pytest Configuration
    # ============================================
    - path: pytest.ini
      size_bytes: 1044
      config_type: testing
      purpose: "Pytest test configuration and markers"

      completeness:
        required_fields:
          total: 4
          present: 4
          missing: []
          coverage_percent: 100
        optional_fields:
          used: [markers, addopts]
          unused: [filterwarnings, norecursedirs]
        settings_documented: true  # Has inline comments
        completeness_score: 34  # 34/35

      correctness:
        syntax_valid: true
        syntax_errors: []
        values_valid: false  # Coverage threshold unrealistic
        invalid_values:
          - field: "--cov-fail-under"
            value: 90
            issue: "Coverage threshold of 90% is unrealistic given actual coverage of 28%"
            expected: "50-60% as incremental target"
        consistency:
          internally_consistent: false
          issues:
            - "Coverage configuration duplicated in pytest.ini and .coveragerc"
        correctness_score: 30  # 30/40 (penalty for unrealistic threshold)

      best_practices:
        follows_standard: true
        standard_name: "pytest best practices"
        compliance_level: partial
        deviations:
          - "Coverage threshold should be achievable"
        security:
          sensitive_data_exposed: false
          concerns: []
        maintainability:
          easy_to_understand: true
          issues:
            - "Coverage config split between pytest.ini and .coveragerc"
        modern_practices:
          uses_modern_format: false  # INI format, could use pyproject.toml
          deprecated_patterns: []
          recommended_updates:
            - "Consider moving all pytest config to pyproject.toml"
        best_practices_score: 18  # 18/25

      settings_audit:
        testpaths: ["tests"]
        python_files: "test_*.py"
        python_classes: "Test*"
        python_functions: "test_*"
        addopts: "--verbose --strict-markers --tb=short --cov=vibey --cov=scripts --cov-report=html --cov-report=term-missing --cov-fail-under=90"
        markers:
          - unit
          - integration
          - e2e
          - platform
          - slow
          - requires_git
          - priority_1
          - priority_2
          - priority_3

      config_quality:
        completeness: 34
        correctness: 30
        best_practices: 18
        total: 82
        grade: B

    # ============================================
    # .coveragerc - Coverage.py Configuration
    # ============================================
    - path: .coveragerc
      size_bytes: 1012
      config_type: coverage
      purpose: "Coverage.py configuration for test coverage measurement"

      completeness:
        required_fields:
          total: 3
          present: 3
          missing: []
          coverage_percent: 100
        optional_fields:
          used: [html, xml, exclude_lines]
          unused: [paths, contexts]
        settings_documented: true
        completeness_score: 33  # 33/35

      correctness:
        syntax_valid: true
        syntax_errors: []
        values_valid: false
        invalid_values:
          - field: "source"
            value: "framework, scripts"
            issue: "Source path 'framework' no longer exists; package is now 'vibey'"
            expected: "vibey, scripts"
          - field: "fail_under"
            value: 90
            issue: "Same unrealistic threshold as pytest.ini"
            expected: "50-60%"
        consistency:
          internally_consistent: true
          issues: []
        correctness_score: 28  # 28/40 (penalty for wrong source path)

      best_practices:
        follows_standard: true
        standard_name: "coverage.py best practices"
        compliance_level: full
        deviations: []
        security:
          sensitive_data_exposed: false
          concerns: []
        maintainability:
          easy_to_understand: true
          issues:
            - "Duplicates some pytest.ini coverage config"
        modern_practices:
          uses_modern_format: false  # INI format
          recommended_updates:
            - "Fix source path to 'vibey'"
            - "Consider moving to pyproject.toml [tool.coverage]"
        best_practices_score: 20  # 20/25

      settings_audit:
        run:
          source: ["framework", "scripts"]  # BUG: should be "vibey"
          omit: ["*/tests/*", "*/test_*", "*/__pycache__/*", "*/venv/*", "*/.venv/*", "*/site-packages/*", "*/.tox/*", "*/conftest.py"]
          branch: true
          parallel: true
        report:
          precision: 2
          show_missing: true
          skip_covered: false
          sort: "Cover"
          fail_under: 90
        html:
          directory: "htmlcov"
          title: "Vibey Framework Test Coverage"

      config_quality:
        completeness: 33
        correctness: 28
        best_practices: 20
        total: 81
        grade: B

    # ============================================
    # .pre-commit-config.yaml - Pre-commit Hooks
    # ============================================
    - path: .pre-commit-config.yaml
      size_bytes: 2241
      config_type: pre_commit
      purpose: "Git pre-commit hooks for code quality enforcement"

      completeness:
        required_fields:
          total: 1
          present: 1
          missing: []
          coverage_percent: 100
        optional_fields:
          used: [repos, hooks with stages]
          unused: [ci, default_install_hook_types]
        settings_documented: true  # Has comments
        completeness_score: 35  # 35/35

      correctness:
        syntax_valid: true
        syntax_errors: []
        values_valid: true
        invalid_values: []
        consistency:
          internally_consistent: false
          issues:
            - "Black line-length (120) differs from pyproject.toml (100)"
        correctness_score: 36  # 36/40

      best_practices:
        follows_standard: true
        standard_name: "pre-commit best practices"
        compliance_level: full
        deviations: []
        security:
          sensitive_data_exposed: false
          concerns: []
        maintainability:
          easy_to_understand: true
          issues: []
        modern_practices:
          uses_modern_format: true
          recommended_updates:
            - "Align Black line-length with pyproject.toml"
            - "Consider pinning hook versions for reproducibility"
        best_practices_score: 22  # 22/25

      hooks_audit:
        repos:
          - repo: "github.com/psf/black"
            rev: "23.11.0"
            hooks: [black]
            purpose: "Python code formatting"
          - repo: "github.com/PyCQA/isort"
            rev: "5.12.0"
            hooks: [isort]
            purpose: "Python import sorting"
          - repo: "github.com/PyCQA/flake8"
            rev: "6.1.0"
            hooks: [flake8]
            purpose: "Python linting"
          - repo: "github.com/pre-commit/pre-commit-hooks"
            rev: "v4.5.0"
            hooks: [check-yaml, end-of-file-fixer, trailing-whitespace, check-merge-conflict, check-added-large-files]
            purpose: "General file quality"
          - repo: local
            hooks:
              - id: pytest-unit
                purpose: "Run unit tests on commit"
              - id: pytest-coverage-check
                purpose: "Check coverage on push"
              - id: check-yaml-python-objects
                purpose: "Prevent Python object serialization in YAML"

        hook_categories:
          formatting: [black, isort, end-of-file-fixer, trailing-whitespace]
          linting: [flake8]
          validation: [check-yaml, check-merge-conflict, check-added-large-files, check-yaml-python-objects]
          testing: [pytest-unit, pytest-coverage-check]

      config_quality:
        completeness: 35
        correctness: 36
        best_practices: 22
        total: 93
        grade: A

    # ============================================
    # .gitignore - Git Ignore Patterns
    # ============================================
    - path: .gitignore
      size_bytes: 1400
      config_type: git
      purpose: "Git ignore patterns for untracked files"

      completeness:
        required_fields:
          total: 0  # .gitignore has no required fields
          present: 0
          missing: []
          coverage_percent: 100
        optional_fields:
          used: ["Python", "IDE", "Testing", "Platform Deployments", "Backups", "Temporary"]
          unused: []
        settings_documented: true  # Has section comments
        completeness_score: 35  # 35/35

      correctness:
        syntax_valid: true
        syntax_errors: []
        values_valid: true
        invalid_values: []
        consistency:
          internally_consistent: true
          issues: []
        correctness_score: 40  # 40/40

      best_practices:
        follows_standard: true
        standard_name: "gitignore best practices"
        compliance_level: full
        deviations: []
        security:
          sensitive_data_exposed: false
          concerns: []
        maintainability:
          easy_to_understand: true
          well_organized: true
          issues: []
        modern_practices:
          uses_modern_format: true
          recommended_updates: []
        best_practices_score: 25  # 25/25

      patterns_audit:
        categories:
          python:
            patterns: ["__pycache__/", "*.py[cod]", "*$py.class", "*.so", ".Python", "build/", "*.egg-info/"]
            complete: true
          ide:
            patterns: [".vscode/", ".idea/", "*.swp", "*.swo", "*~", ".DS_Store"]
            complete: true
          testing:
            patterns: [".pytest_cache/", ".coverage", "htmlcov/", ".tox/"]
            complete: true
          build:
            patterns: ["build/", "dist/", "downloads/", "eggs/", ".eggs/", "wheels/"]
            complete: true
          environment:
            patterns: ["venv/", "ENV/", "env/", ".venv"]
            complete: true
          project_specific:
            patterns: [".claude", ".goose/", ".cursor/", ".vibey/config-backups/", ".vibey-checkpoints/", ".vibey/roadmap/01*/"]
            documented: true

        missing_patterns: []
        over_ignoring: []

      config_quality:
        completeness: 35
        correctness: 40
        best_practices: 25
        total: 100
        grade: A

    # ============================================
    # MANIFEST.in - Package Manifest
    # ============================================
    - path: MANIFEST.in
      size_bytes: 86
      config_type: packaging
      purpose: "Package distribution manifest for sdist"

      completeness:
        required_fields:
          total: 0
          present: 0
          missing: []
          coverage_percent: 100
        optional_fields:
          used: [include, recursive-include]
          unused: [exclude, graft, prune, recursive-exclude]
        settings_documented: false
        completeness_score: 25  # 25/35 (minimal entries)

      correctness:
        syntax_valid: true
        syntax_errors: []
        values_valid: true
        invalid_values: []
        consistency:
          internally_consistent: true
          issues: []
        correctness_score: 40  # 40/40

      best_practices:
        follows_standard: true
        standard_name: "MANIFEST.in best practices"
        compliance_level: partial
        deviations:
          - "Missing framework content files (*.md, *.yaml in framework/)"
        security:
          sensitive_data_exposed: false
          concerns: []
        maintainability:
          easy_to_understand: true
          issues:
            - "Very minimal - may be missing files"
        modern_practices:
          uses_modern_format: true
          recommended_updates:
            - "Add framework content files if needed in distribution"
            - "Add CHANGELOG.md, CONTRIBUTING.md"
        best_practices_score: 16  # 16/25

      directives_audit:
        include:
          - README.md
          - LICENSE
          - vibey/py.typed
        recursive_include:
          - "vibey *.py"
        exclude: []
        graft: []
        prune: []

      package_completeness:
        includes_all_needed: false
        missing:
          - CHANGELOG.md
          - CONTRIBUTING.md
          - "framework/**/*.md (if distributing content)"
        unnecessary: []

      config_quality:
        completeness: 25
        correctness: 40
        best_practices: 16
        total: 81
        grade: B

  # ============================================
  # Cross-File Analysis
  # ============================================
  cross_file_analysis:
    consistency:
      python_version_consistent: true
      versions_found:
        pyproject.toml: ">=3.9"
        tool.black: "py39"
        tool.ruff: "py39"
        tool.mypy: "3.9"

      line_length_consistent: false
      line_lengths_found:
        pyproject.toml_black: 100
        pyproject.toml_ruff: 100
        pre_commit_black: 120
        pre_commit_flake8: 120
      issue: "Black uses 100 in pyproject.toml but 120 in pre-commit"

      coverage_threshold_consistent: true
      coverage_thresholds:
        pytest.ini: 90
        coveragerc: 90
      issue: "Both set to 90% but actual coverage is 28%"

    redundancy:
      duplicated_settings:
        - setting: "pytest test paths"
          locations: ["pyproject.toml", "pytest.ini"]
          recommendation: "Keep only in pyproject.toml"
        - setting: "coverage configuration"
          locations: ["pytest.ini", ".coveragerc"]
          recommendation: "Consolidate in pyproject.toml [tool.coverage]"

    gaps:
      missing_configs:
        - name: ".editorconfig"
          purpose: "Editor-agnostic formatting"
          priority: low
        - name: "pyrightconfig.json or similar"
          purpose: "Type checking in IDE"
          priority: low

  # ============================================
  # Overall Recommendations
  # ============================================
  recommendations:
    critical:
      - issue: ".coveragerc source path incorrect"
        file: .coveragerc
        current: "source = framework"
        fix: "source = vibey"
        priority: high

      - issue: "Coverage threshold unrealistic"
        files: [pytest.ini, .coveragerc]
        current: "fail_under = 90"
        fix: "fail_under = 50 (increase incrementally)"
        priority: high

    important:
      - issue: "Line length inconsistency"
        files: [pyproject.toml, .pre-commit-config.yaml]
        fix: "Standardize on 100 or 120 across all tools"
        priority: medium

      - issue: "Duplicated pytest config"
        files: [pyproject.toml, pytest.ini]
        fix: "Consolidate all pytest config in pyproject.toml"
        priority: medium

    nice_to_have:
      - issue: "MANIFEST.in minimal"
        file: MANIFEST.in
        fix: "Add missing files if needed for distribution"
        priority: low

      - issue: "No .editorconfig"
        fix: "Add .editorconfig for cross-IDE consistency"
        priority: low
