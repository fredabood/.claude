
# step 1
create the computed database using the tooling we've built in this sprint;

create second database that has all the same tables and column as the first one but no views or computed fields; iterate through every yaml file and insert all fields into their appropriate location in the sql database; the result should be a sqlite database with 100% of the data materialized and a perfect match to the current state of the roadmap yaml;

compare the computed database to the non-computed database to and generate a detailed report identifying all gaps in data integrity, including areas where the computed database could be wrong or where the yaml state is incorrect;

# step 2

dump the computed database to a new copy of the yaml roadmap and compare the generate yaml to the original yaml roadmaps;
  - if 100% of the data in the original yaml files is represented in the yaml generated by the sqlite database, stop; if not identical, generate a comprehensive audit report of the differences, and plan next steps with the user

# step 3

read the generated copy of the yaml into a new computed database, compare this computed database to the declared databse from step one to confirm they're identical
