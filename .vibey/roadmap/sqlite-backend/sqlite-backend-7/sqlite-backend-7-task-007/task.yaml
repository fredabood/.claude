task:
  id: sqlite-backend-7-task-007
  sprint_id: sqlite-backend-7
  track_id: sqlite-backend
  roadmap_id: vibey-framework-v2
  task_type: development
  title: Integrate Artifact System with Ticket Layers
  description: |
    Add artifact-aware methods and properties to Ticket, HierarchicalTicket,
    and domain model layers.

    DESIGN REFERENCE: UNIFIED_TICKET_ARCHITECTURE.md Part 13.8

    LAYER 1: TICKET INTEGRATION

    Add to Ticket class:

    @property
    def artifact_criteria(self) -> List[Criterion]:
        """Criteria that reference artifacts."""
        return [
            c for c in self.criteria
            if isinstance(c.target, ArtifactTarget)
        ]

    @property
    def referenced_artifact_ids(self) -> List[str]:
        """IDs of artifacts referenced by this ticket's criteria."""
        return [
            c.target.artifact_id
            for c in self.artifact_criteria
        ]

    Update complete() method:
        # Additional check: all documentation artifacts must be current
        for criterion in self.artifact_criteria:
            if criterion.target.verification == ArtifactVerification.NOT_STALE:
                if criterion.target.artifact_is_stale:
                    reasons.append(
                        f"Documentation artifact is stale: {criterion.description}"
                    )
                    can = False

        if can:
            # Capture artifact hashes for future impact analysis
            self._capture_artifact_state()

    LAYER 2: HIERARCHICAL TICKET INTEGRATION

    Add to HierarchicalTicket class:

    @property
    def all_referenced_artifacts(self) -> List[str]:
        """All artifact IDs referenced by this ticket and descendants."""
        if self.is_ultimate_child:
            return self.referenced_artifact_ids
        all_ids = list(self.referenced_artifact_ids)
        for child in self._load_children():
            all_ids.extend(child.all_referenced_artifacts)
        return list(set(all_ids))

    @property
    def stale_documentation_artifacts(self) -> List[str]:
        """Artifact IDs for stale documentation in this subtree."""
        stale = []
        for criterion in self.artifact_criteria:
            if (criterion.target.verification == ArtifactVerification.NOT_STALE
                and criterion.target.artifact_is_stale):
                stale.append(criterion.target.artifact_id)
        if not self.is_ultimate_child:
            for child in self._load_children():
                stale.extend(child.stale_documentation_artifacts)
        return list(set(stale))

    @property
    def has_stale_documentation(self) -> bool:
        """True if any documentation in this subtree is stale."""
        return len(self.stale_documentation_artifacts) > 0

    @property
    def documentation_health(self) -> DocumentationHealth:
        """Aggregate documentation health status."""
        stale_count = len(self.stale_documentation_artifacts)
        if stale_count == 0:
            return DocumentationHealth.HEALTHY
        # Check if any stale docs block completion
        for criterion in self.artifact_criteria:
            if (criterion.target.artifact_is_stale
                and criterion.blocks_transition_to == TicketStatus.COMPLETED
                and criterion.required):
                return DocumentationHealth.CRITICAL
        return DocumentationHealth.DEGRADED

    LAYER 3: DOMAIN MODEL INTEGRATION

    Add to RoadmapTicket:

    @property
    def all_project_documentation(self) -> List[str]:
        """All DOCUMENTATION type artifacts across the roadmap."""
        return [
            aid for aid in self.all_referenced_artifacts
            if self._get_artifact_type(aid) == ArtifactType.DOCUMENTATION
        ]

    @property
    def framework_components(self) -> List[str]:
        """All AGENT, WORKFLOW, TEMPLATE artifacts."""
        return [
            aid for aid in self.all_referenced_artifacts
            if self._get_artifact_type(aid) in {
                ArtifactType.AGENT,
                ArtifactType.WORKFLOW,
                ArtifactType.TEMPLATE
            }
        ]

    @property
    def orphan_artifacts(self) -> List[str]:
        """Artifacts that exist but aren't referenced by any ticket."""
        return self._artifact_registry.get_orphans()

    Add to SprintTicket:

    @property
    def sprint_context_artifacts(self) -> List[str]:
        """CONTEXT type artifacts for this sprint (planning docs, etc.)."""
        return [
            c.target.artifact_id for c in self.artifact_criteria
            if self._get_artifact_type(c.target.artifact_id) == ArtifactType.CONTEXT
        ]

    @property
    def planning_artifacts(self) -> List[str]:
        """Planning documents (PLANNING_DOC subtype)."""
        return [
            aid for aid in self.sprint_context_artifacts
            if self._get_artifact_subtype(aid) == ContextArtifactSubtype.PLANNING_DOC
        ]

    FILE CHANGES:
    - vibey/roadmap/models/ticket/ticket.py (Layer 1)
    - vibey/roadmap/models/ticket/hierarchical_ticket.py (Layer 2)
    - vibey/roadmap/models/ticket/domain_models.py (Layer 3)
    - tests/roadmap/models/ticket/test_artifact_integration.py

    SUCCESS CRITERIA:
    - artifact_criteria property works on Ticket
    - complete() checks for stale documentation
    - all_referenced_artifacts aggregates correctly
    - stale_documentation_artifacts aggregates correctly
    - documentation_health computed correctly
    - Domain-specific accessors work
    - Full test coverage
  status: not_started
  blocked: false
  created: '2025-11-30T18:00:00+00:00'
  started: null
  completed: null
  assigned_agent: backend-engineer
  priority: high
  phase_label: integration
  estimated_tokens: 3000
  actual_tokens: null
  complexity: high
  gate_info: null
  audit_results: null
  dependencies: []
  blocks:
  - sqlite-backend-7-task-008
  blocked_by:
  - sqlite-backend-7-task-002
  - sqlite-backend-7-task-004
  - sqlite-backend-7-task-006
  depends_on:
  - blocker_id: sqlite-backend-7-task-002
    blocker_type: task
    required_status: completed
    current_status: not_started
    blocks_transition_to: in_progress
    last_checked: '2025-11-30T18:00:00+00:00'
  - blocker_id: sqlite-backend-7-task-004
    blocker_type: task
    required_status: completed
    current_status: not_started
    blocks_transition_to: in_progress
    last_checked: '2025-11-30T18:00:00+00:00'
  - blocker_id: sqlite-backend-7-task-006
    blocker_type: task
    required_status: completed
    current_status: not_started
    blocks_transition_to: in_progress
    last_checked: '2025-11-30T18:00:00+00:00'
  depended_on_by:
  - sqlite-backend-7-task-008
  deliverables:
  - vibey/roadmap/models/ticket/ticket.py
  - vibey/roadmap/models/ticket/hierarchical_ticket.py
  - vibey/roadmap/models/ticket/domain_models.py
  - tests/roadmap/models/ticket/test_artifact_integration.py
  commits: []
  metadata:
    last_updated: '2025-11-30T18:00:00+00:00'
    token_efficiency: null
    duration_hours: null
    notes: |
      Layer integration ensures artifacts are first-class citizens
      throughout the ticket hierarchy.

      Key behaviors:
      - Completion blocked by stale documentation (if required)
      - Parents aggregate artifacts from children
      - Domain models provide semantic accessors

      DocumentationHealth provides a quick health check without
      walking the entire subtree.
