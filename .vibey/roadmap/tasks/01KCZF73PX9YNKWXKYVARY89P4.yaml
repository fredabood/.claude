task:
  id: 01KCZF73PX9YNKWXKYVARY89P4
  name: Implement CommitFrequencyEnforcer for disciplined commits
  description: |
    Create system to enforce commit frequency during execution:

    ```python
    class CommitFrequencyEnforcer:
        def __init__(self, config: ImplementConfig):
            self.config = config
            self.last_commit_at: Optional[datetime] = None
            self.uncommitted_changes: List[Path] = []
            self.commit_count: int = 0

        def check_commit_needed(self) -> CommitCheck:
            '''
            Check if a commit is needed based on:
            - Time since last commit
            - Number of files changed
            - Lines of code changed
            - Logical completion points
            '''

        def get_uncommitted_changes(self) -> List[Path]:
            '''Get list of files with uncommitted changes.'''

        def should_commit_now(self) -> bool:
            '''
            Determine if commit needed now.

            Triggers:
            - Max time between commits exceeded
            - Max files changed exceeded
            - Logical breakpoint reached
            - Approaching token limit
            '''

        def create_wip_commit(self) -> str:
            '''
            Create work-in-progress commit.

            Message format:
            wip({ticket_id}): Work in progress

            Changes:
            - file1.py
            - file2.py

            Progress: 60% complete
            '''

        def create_logical_commit(
            self,
            description: str,
            files: Optional[List[Path]] = None,
        ) -> str:
            '''
            Create commit at logical breakpoint.

            Message format:
            feat({ticket_id}): {description}
            '''

        def get_commit_stats(self) -> CommitStats:
            '''Get statistics about commits this session.'''
    ```

    Frequency configuration:
    ```yaml
    implement:
      git:
        commit_frequency:
          max_minutes_between: 15
          max_files_changed: 10
          max_lines_changed: 500
          on_subtask_complete: true
          wip_on_pause: true
    ```

    Integration with loop:
    ```python
    async def run(self):
        while not self.should_stop():
            # ... execute task ...

            # Check commit frequency
            if self.commit_enforcer.should_commit_now():
                self.commit_enforcer.create_wip_commit()

            # After task complete
            self.commit_enforcer.create_logical_commit(
                f"Complete: {task.name}"
            )
    ```

    Files:
    - vibey/services/implementation/git/commit_enforcer.py (new)
  format_version: v2
  ticket_type: task
  parent_ref: 01KCZF73PX9YNKWXKYVARY89P2
  status: not_started
  created_at: '2025-12-20T17:00:00+00:00'
  started_at: null
  completed_at: null
  updated_at: '2025-12-20T17:00:00+00:00'
  assigned_agents:
  - unassigned
  priority: high
  criteria:
  - type: artifact
    description: vibey/services/implementation/git/commit_enforcer.py exists
    status: not_met
  - type: code
    description: Commits created at configured frequency
    status: not_met
  - type: code
    description: WIP commits for long-running tasks
    status: not_met
  - type: code
    description: Logical commits at breakpoints
    status: not_met
  depends_on:
  - 01KCZF73PX9YNKWXKYVARY89P3
  commits_local: []
  requirements_local: []
  estimated_duration: null
  deferred: false
  task_type_detail: development
  estimated_tokens: 1
  actual_tokens: null
  complexity: medium
  phase_label: null
  gate_info: null
  audit_results: null
  metadata:
    last_updated: null
    token_efficiency: null
    duration_hours: null
