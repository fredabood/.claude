task:
  id: unified-arch-1-task-002
  sprint_id: unified-arch-1
  track_id: unified-architecture-migration
  roadmap_id: vibey-framework-v2
  task_type: development
  title: Create directory migration script with backup
  description: |
    Create a migration script that:
    1. Backs up the entire .vibey/roadmap/ directory
    2. Creates new flat directory structure (tracks/, sprints/, tasks/, artifacts/, context/)
    3. Moves files using git mv to preserve history
    4. Updates internal file references
    5. Validates the migration
    6. Provides rollback capability

    IMPLEMENTATION:
    ```python
    def migrate_to_flat_structure(
        root_dir: Path,
        dry_run: bool = True,
        backup: bool = True
    ) -> MigrationResult:
        # 1. Create backup
        if backup:
            backup_path = create_backup(root_dir)

        # 2. Scan current structure
        tracks, sprints, tasks = scan_nested_structure(root_dir)

        # 3. Create new directories
        create_flat_directories(root_dir)

        # 4. Move files with git mv
        for track in tracks:
            git_mv(track.path, root_dir / "tracks" / f"{track.id}.yaml")
        # ... sprints, tasks

        # 5. Update references
        update_yaml_references(root_dir)

        # 6. Validate
        validate_migration(root_dir)

        return MigrationResult(...)
    ```

    CLI COMMAND:
    vibey roadmap migrate-structure --dry-run
    vibey roadmap migrate-structure --execute
    vibey roadmap migrate-structure --rollback
  status: not_started
  blocked: false
  created: '2025-12-07T15:00:00+00:00'
  started: null
  completed: null
  assigned_agent: null
  priority: high
  phase_label: implementation
  estimated_tokens: 50000
  actual_tokens: null
  complexity: complex
  gate_info: null
  audit_results: null
  dependencies: []
  blocks:
  - unified-arch-1-task-005
  blocked_by:
  - unified-arch-1-task-001
  depends_on:
  - blocker_id: unified-arch-1-task-001
    blocker_type: task
    required_status: completed
    description: Need directory schema before implementing migration
    status: pending
  depended_on_by: []
  deliverables:
  - path: vibey/roadmap/serialization/directory_migration.py
    type: code
    description: Directory migration script
  commits: []
  metadata:
    last_updated: null
    token_efficiency: null
    duration_hours: null
